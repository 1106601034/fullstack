namespace TaskManager.Domain.Enums;

/// <summary>
/// Represents task priority levels.
/// </summary>
public enum TaskPriority
{
    Low = 0,
    Medium = 1,
    High = 2,
    Critical = 3,
}
